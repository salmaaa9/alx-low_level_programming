#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 *main -entry point
 *Description:check no is positive negative or zero
 *Return: 0 (success)
*/
int main(void)
{
	int i;

	for (i = 122; i > 96; i--)
	{
		putchar(i);
	}
	putchar(10);
	return (0);
}
